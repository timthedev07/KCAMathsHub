import { getMetadata } from "../../../../lib/getMetadata";
export const metadata = getMetadata({
  title: "Initial release",
  sectionPrepend: "Updates & changelog",
});

# Initial Release!

Read about everything you need to know [here](/docs)!
